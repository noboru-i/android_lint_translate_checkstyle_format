require 'spec_helper'

describe AndroidLintTranslateCheckstyleFormat do
  it 'has a version number' do
    expect(AndroidLintTranslateCheckstyleFormat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
